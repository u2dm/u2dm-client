import {
    Button,
    LineEdit,
    Palette,
} from "std-widgets.slint";

import { LoginRequest } from "./types.slint";
import { BrandTitle, StatusText, ErrorText, LoginStatus } from "./common.slint";
import { LoginMethodKind } from "../../enums.slint";
import { LoginView } from "../../views.slint";

export component CredentialsScreen inherits VerticalLayout {
    alignment: center;
    padding: 24px;
    spacing: 8px;

    in-out property <string> input-username: "";
    in-out property <string> input-password: "";

    private property <bool> is-logging-in: LoginView.status != "";
    private property <bool> is-oauth-pending: LoginView.status == "opening-browser" || LoginView.status == "waiting-auth";

    callback login-password(LoginRequest);
    callback login-oauth();
    callback cancel-oauth();
    callback back();

    function submit-password() {
        if (!root.is-logging-in && root.input-username != "" && root.input-password != "") {
            root.login-password({
                homeserver: LoginView.resolved-homeserver,
                username: root.input-username,
                password: root.input-password,
            });
        }
    }

    BrandTitle { }

    Text {
        text: @tr("Server: {}", LoginView.resolved-homeserver);
        font-size: 14px;
        horizontal-alignment: center;
        color: Palette.foreground.transparentize(0.3);
    }

    if LoginView.method == LoginMethodKind.password || LoginView.method == LoginMethodKind.both: VerticalLayout {
        spacing: 8px;

        HorizontalLayout {
            alignment: center;

            LineEdit {
                text <=> root.input-username;
                placeholder-text: @tr("Username");
                horizontal-stretch: 0;
                max-width: root.width;
                enabled: !root.is-logging-in;
                accepted => {
                    root.submit-password();
                }
            }
        }

        HorizontalLayout {
            alignment: center;

            LineEdit {
                text <=> root.input-password;
                placeholder-text: @tr("Password");
                input-type: password;
                horizontal-stretch: 0;
                max-width: root.width;
                enabled: !root.is-logging-in;
                accepted => {
                    root.submit-password();
                }
            }
        }

        HorizontalLayout {
            alignment: center;

            Button {
                text: root.is-logging-in ? @tr("Logging in...") : @tr("Log in");
                max-width: root.width;
                enabled: !root.is-logging-in && root.input-username != "" && root.input-password != "";
                clicked => {
                    root.submit-password();
                }
            }
        }
    }

    if LoginView.method == LoginMethodKind.oauth || LoginView.method == LoginMethodKind.both: HorizontalLayout {
        alignment: center;

        Button {
            text: @tr("Log in with browser");
            enabled: !root.is-logging-in;
            clicked => {
                root.login-oauth();
            }
        }
    }

    if root.is-oauth-pending: HorizontalLayout {
        alignment: center;

        Button {
            text: @tr("Cancel");
            clicked => {
                root.cancel-oauth();
            }
        }
    }

    HorizontalLayout {
        alignment: center;

        Button {
            text: @tr("Back");
            enabled: !root.is-logging-in;
            clicked => {
                root.back();
            }
        }
    }

    if LoginView.status != "": StatusText {
        text: LoginStatus.label(LoginView.status);
    }

    if LoginView.error != "": ErrorText {
        text: LoginView.error;
    }
}
