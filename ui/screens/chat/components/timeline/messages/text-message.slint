import { Palette } from "std-widgets.slint";

import { Fonts } from "../../../../../theme.slint";

export component TextMessage inherits TextInput {
    font-family: Fonts.emoji;
    font-size: 14px;
    wrap: word-wrap;
    read-only: true;
    single-line: false;
    color: Palette.foreground;
}
