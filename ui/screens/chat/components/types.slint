export struct RoomEntry {
    id: string,
    name: string,
    unread: int,
    mentions: int,
    last-message-sender: string,
    last-message-kind: string,
    last-message-body: string,
    last-message-is-own: bool,
    last-message-time: string,
}

export struct MessageEntry {
    unique-id: string,
    sender: string,
    body: string,
    timestamp: string,
    message-type: string,
    has-thumbnail: bool,
    thumbnail: image,
    image-width: int,
    image-height: int,
    event-id: string,
    has-avatar: bool,
    avatar: image,
    sender-initial: string,
    is-own: bool,
}

export struct SendMessageRequest {
    room-id: string,
    body: string,
}

export struct SaveFileRequest {
    event-id: string,
    filename: string,
}
