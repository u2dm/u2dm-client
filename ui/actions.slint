import { LoginRequest } from "screens/login/types.slint";
import {
    SendMessageRequest,
    SendStickerRequest,
    SendAttachmentRequest,
    SaveFileRequest,
} from "screens/chat/components/types.slint";

export global Actions {
    callback check-server(string);
    callback login-password(LoginRequest);
    callback login-oauth();
    callback cancel-oauth();
    callback back-to-homeserver();
    callback reauth-password(string);
    callback reauth-oauth();
    callback logout();

    callback select-room(string);
    callback select-space(string);
    callback select-direct();
    callback select-subspace(string);
    callback move-space(int, int);
    callback request-room-avatar(string);
    callback open-space-index();
    callback close-space-index();
    callback page-space-index();
    callback retry-space-index();
    callback join-space-child(string);
    callback open-space-child(string);

    callback send-message(SendMessageRequest);
    callback dismiss-unsent(int);
    callback send-sticker(SendStickerRequest);
    callback pick-photo(string);
    callback pick-document(string);
    callback send-attachment(SendAttachmentRequest);
    callback cancel-attachment();
    callback request-sticker(string);
    callback search-stickers(string);
    callback open-media(string);
    callback open-video(string);
    callback close-video();
    callback toggle-video();
    callback toggle-video-muted();
    callback seek-video(int);
    callback play-audio(string);
    callback toggle-audio();
    callback seek-audio(int);
    callback close-audio();
    callback open-link(string);
    callback save-file(SaveFileRequest);
    callback request-media(string);
    callback scroll-position-changed(bool, int);
    callback timeline-visibility-changed(bool);
    callback paginate-backwards();
    callback paginate-forwards();
    callback jump-to-latest();
    callback jump-to-event(string);
    callback toggle-reaction(string, string);
    callback retry-send(string);
    callback discard-send(string);
    callback retry-timeline();
    callback dismiss-toast();

    callback accept-verification();
    callback confirm-verification();
    callback reject-verification();
    callback dismiss-verification();
}
