export enum LoginPhase {
    loading,
    homeserver,
    credentials,
    reauthenticate,
    logged-in,
}

export enum LoginActivity {
    idle,
    loading-session,
    opening-store,
    connecting,
    restoring-auth,
    checking-server,
    logging-in,
    opening-browser,
    waiting-auth,
    syncing,
    cleaning-up,
}

export enum ConnectionState {
    disconnected,
    connecting,
    connected,
    error,
}

export enum TimelineState {
    none,
    loading,
    loading-unread,
    loading-focus,
    ready,
    failed,
    disconnected,
}

export enum VerificationPhase {
    none,
    requested,
    emojis,
    confirming,
    done,
    cancelled,
}

export enum VerificationActivity {
    none,
    accepting,
    declining,
    confirming,
}

export enum UserMessageKind {
    none,
    server-unreachable,
    unsupported-login-method,
    login-failed,
    invalid-credentials,
    account-deactivated,
    invalid-username,
    rate-limited,
    login-method-unsupported,
    session-unreadable,
    session-restore-failed,
    store-key-missing,
    store-key-unreadable,
    identity-diverged,
    session-expired,
    reauth-not-possible,
    data-quarantined,
    data-not-erased,
    interrupted-login-unresolved,
    another-instance-running,
    local-data-claim-failed,
    session-save-failed,
    send-message-failed,
    load-more-failed,
    message-not-found,
    message-not-showable,
    space-order-save-failed,
    join-room-failed,
    media-download-failed,
    file-download-failed,
    media-open-failed,
    media-not-viewable,
    attachment-unreadable,
    attachment-too-large,
    send-attachment-failed,
    video-playback-failed,
    audio-playback-failed,
    file-save-failed,
    file-saved,
    verification-accept-failed,
    verification-confirm-failed,
    verification-reject-failed,
    verification-timed-out,
    verification-sas-accept-failed,
    verification-cancelled,
    verification-declined,
    verification-mismatch,
    verification-accepted-elsewhere,
    verification-failed,
}

export enum SendState {
    sent,
    sending,
    uploading,
    failed,
}

export enum Delivery {
    none,
    pending,
    sent,
    read,
    seen,
    failed,
}

export enum ReactionSend {
    sent,
    sending,
    failed,
}

export enum AttachmentKind {
    file,
    image,
    video,
    audio,
}

export enum RoomScope {
    all,
    direct,
    space,
}

export enum SpaceIndexStatus {
    closed,
    loading,
    partial,
    complete,
    loading-more,
    failed,
    more-failed,
}

export enum ChildAccess {
    open,
    open-subspace,
    joined,
    joining,
    join,
    invite-only,
    knock,
    members-only,
    unavailable,
}

export enum MediaState {
    idle,
    ready,
    failed,
}

export enum MediaFailure {
    none,
    no-source,
    download,
    too-large,
    storage,
    unreadable,
    unsupported-format,
    too-large-to-display,
    damaged,
}

export enum MessageKind {
    text,
    notice,
    emote,
    image,
    video,
    audio,
    sticker,
    file,
    service,
    utd,
    unsupported,
}

export enum PreviewKind {
    none,
    text,
    image,
    video,
    audio,
    voice,
    file,
    location,
    encrypted,
    sticker,
    poll,
}

export enum AudioKind {
    voice,
    track,
}

export enum ServiceKind {
    none,
    joined,
    left,
    invited,
    invitation-accepted,
    invitation-rejected,
    invitation-revoked,
    kicked,
    banned,
    unbanned,
    knocked,
    knock-accepted,
    name-set,
    name-changed,
    name-removed,
    avatar-changed,
    room-name,
    room-topic,
    room-avatar,
    room-created,
    encryption,
    call-started,
    call-notification,
}

export enum LoginMethodKind {
    none,
    password,
    oauth,
    both,
}
